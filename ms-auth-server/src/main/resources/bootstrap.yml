server:
  port: 10001
spring:
  application:
    name: auth
# eureka:
#   client:
#     service-url:
#       defaultZone: http://localhost:8761/eureka/    # 指定注册中心的地址
grpc.server.port: ${GRPC_SERVER_PORT}
     
      